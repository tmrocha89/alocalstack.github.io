// Minimal shared test setup for Node environment.
// No DOM / jsdom dependencies to maintain compatibility with Node 18.

// You can still test browser-only code by mocking globals per test file when needed.